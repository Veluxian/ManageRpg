import { Link } from "@remix-run/react";

export default function creacionPersonajes() {
    return(
        <div>
            <p>Llegamos</p>
            <Link to="/."> Volvamos </Link>
        </div>
    );
}